# Pruebas para verificar fix del proxy streaming
# Ejecutar DESPUÉS de reconstruir el proxy

# 1. Reconstruir el proxy
docker compose up -d --build docker-socket-proxy

# 2. Esperar a que PZ esté healthy
docker ps | grep project-zomboid

# 3. Probar comando players desde el bot (debería devolver lista de jugadores)
docker exec pz-telegram-bot docker exec project-zomboid rcon-cli -c /home/steam/server/rcon.yml players

# 4. Verificar en logs del proxy que se usó streaming
docker logs pz-docker-proxy --tail 20 | grep -i "streaming"
